# TypeScript with React

This project is created to show my integration skills on TypeScript with React.
You definitely can start this project by running `npm start`, but it has no serious logic, so the code is more important.

